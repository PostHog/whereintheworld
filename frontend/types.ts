// TODO
export interface TripType {
    destination: string
}

export interface TripMatchType {
    avatarUrl: string
    personName: string
}

export interface CityType {
    id: number
    name: string
    latitude: number
    longitude: number
    country_code: string
    timezone: string
}
